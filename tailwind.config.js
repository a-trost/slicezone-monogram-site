const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    colors: {
      navy: '#1a2456',
      orange: '#ff501c',
      black: '#080b1b',
      peach: '#ff8b68',
      offWhite: '#f0efed',
    },
    fontFamily: {
      // sans: ['Graphik', 'sans-serif'],
      // serif: ['Merriweather', 'serif'],
    },
  },
}
